<template>
  <div id="app">
    <button class="compute-button"
    @mousedown="onButtonClick(true)"
    @mouseup="onButtonClick(false)"
    :disabled="isDisabled"
    >{{ titlec }}</button>
  </div>
</template>

<script setup>
import { ref } from "vue";

const emits = defineEmits(["toggleCompute"]);

const props = defineProps(["title", "isDisabled"]);
const titlec = ref(props.title);
const isActive = ref(false);


async function onButtonClick(state) {
  isActive.value = state
  emits("toggleCompute", isActive.value );
}
</script>

<style scoped>

button,   .customUpload {
    width: 100%;
    font-family: 'Helvetica', monospace;
    background-color: var(--red);
    border: #fff018;
    color: rgb(255, 255, 255);
    font-weight: bold;
    padding: 10px 15px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 4px 2px;
    cursor: pointer;
    border-radius: 10px;
    transition: all 0.1s ease-in-out;
    
  }
  
  
  button:hover,  .customUpload:hover {
    background-color: #125650;
    box-shadow: 3px 3px rgba(46, 46, 46, 0.799);
  }

  button, .customUpload {
    /* Other styles... */
    background-color: #b11244; /* New color */
    /* Other styles... */
}


</style>